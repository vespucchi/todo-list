import '../styles/newTaskModal-style.css';

export default function createNewTaskModal() {
    const modal = document.createElement('dialog');
    const body = document.querySelector('body');

    
    body.append(modal);
}